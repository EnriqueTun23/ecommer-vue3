export interface CartDetail {
    productId: number;
    quantity: number;
}

export interface Product {
    name: string,
    price: number,
    id: number,
}

export interface Categories {
    name: string,
    id: number,
}